# Mixture-of-Agents Consensus Audit Protocol

Scoped to high-stakes promotions only (approving a trajectory for RLVR training upload, or a policy change to Tier 4) — not every step needs this, that's what the single Official/Red Auditor pair (`00_DEFINITIVE_MASTER_SPECIFICATION_V3_COMPLETE.md` §7) is for.

**Fail-closed rule**: if the consensus panel can't reach agreement, the default is reject/quarantine, never a majority-of-one pass-through. An unresolved audit is a failed audit, not a pending success.

Source content for the panel composition and voting mechanics itself was not captured this pass — this file states the rule the title promises (fail-closed) from context, not a fully sourced spec; verify against the live Drive doc before treating the mechanics as final.
